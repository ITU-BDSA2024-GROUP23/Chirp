name: Prepare pipeline and set variables

on:
  workflow_call:
    outputs:
      deploy_env:
        description: Deployment environment (prod, stag or dev)
        value: ${{ jobs.prepare-pipeline-vars.outputs.deploy_env }}

      package_version_cli:
        description: The version of the package
        value: ${{ jobs.prepare-pipeline-vars.outputs.package_version_CLI }}

      package_version_csvdb:
        description: The version of the package
        value: ${{ jobs.prepare-pipeline-vars.outputs.package_version_CLI }}

jobs:
  prepare-pipeline-vars:
    runs-on: ubuntu-latest

    outputs:
      deploy_env: ${{ steps.pipeline-vars.outputs.DEPLOY_ENV }}
      package_version_cli: ${{ steps.pipeline-vars.outputs.PACKAGE_VERSION_CLI }}
      package_version_csvdb: ${{ steps.pipeline-vars.outputs.PACKAGE_VERSION_CSVDB }}

    permissions:
      contents: write

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v4

      - name: Get deployment variables
        id: pipeline-vars
        env:
          BRANCH: ${{ github.ref_name }}
        run: |
          echo "Checking deployment environment..."

          if [[ $BRANCH == "main" || $BRANCH == "master" ]]; then
            DEPLOY_ENV="prod"
          elif [[ $BRANCH == "staging" ]]; then
            DEPLOY_ENV="stag"
          else
            DEPLOY_ENV="dev"
          fi

          echo "DEPLOY_ENV=$DEPLOY_ENV"
          echo "DEPLOY_ENV=$DEPLOY_ENV" >> $GITHUB_OUTPUT


          echo "Checking package version..."

          if [[ $DEPLOY_ENV == "stag" ]]; then
            PACKAGE_VERSION_SUFFIX="-$(git rev-parse --short HEAD)"
          else
            PACKAGE_VERSION_SUFFIX=""
          fi

          PACKAGE_VERSION_CLI="$(sed -n 's/.*<PackageVersion>\(.*\)<\/PackageVersion>.*/\1/p' src/Chirp.CLI/Chirp.CLI.csproj)"
          echo "PACKAGE_VERSION_CLI=$PACKAGE_VERSION_CLI$(echo $PACKAGE_VERSION_SUFFIX)"
          echo "PACKAGE_VERSION_CLI=$PACKAGE_VERSION_CLI$(echo $PACKAGE_VERSION_SUFFIX)" >> $GITHUB_OUTPUT

          PACKAGE_VERSION_CSVDB="$(sed -n 's/.*<PackageVersion>\(.*\)<\/PackageVersion>.*/\1/p' src/Chirp.CSVDBService/Chirp.CSVDBService.csproj)"
          echo "PACKAGE_VERSION_CSVDB=$PACKAGE_VERSION_CSVDB$(echo $PACKAGE_VERSION_SUFFIX)"
          echo "PACKAGE_VERSION_CSVDB=$PACKAGE_VERSION_CSVDB$(echo $PACKAGE_VERSION_SUFFIX)" >> $GITHUB_OUTPUT

      - name: Write pipeline vars summary
        run: |
          echo "### Prepare pipeline:
          <table>
            <tr>
              <td>🎯 Deployment environment</td>
              <td>${{ steps.pipeline-vars.outputs.DEPLOY_ENV }}</td>
            </tr>
            <tr>
              <td>📦 CLI version</td>
              <td>${{ steps.pipeline-vars.outputs.PACKAGE_VERSION_CLI }}</td>
            </tr>
            <tr>
              <td>📦 CSVDB version</td>
              <td>${{ steps.pipeline-vars.outputs.PACKAGE_VERSION_CSVDB }}</td>
            </tr>
          </table>" >> $GITHUB_STEP_SUMMARY

  prepare-azure:
    if: |
      needs.prepare-pipeline-vars.outputs.deploy_env == 'prod' ||
      needs.prepare-pipeline-vars.outputs.deploy_env == 'stag'
    runs-on: ubuntu-latest

    needs: prepare-pipeline-vars

    environment: ${{ needs.prepare-pipeline-vars.outputs.deploy_env }}

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Validate login to Azure using OIDC
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}

      - name: Azure CLI show account
        uses: azure/cli@v2
        with:
          azcliversion: latest
          inlineScript: |
            az account show
